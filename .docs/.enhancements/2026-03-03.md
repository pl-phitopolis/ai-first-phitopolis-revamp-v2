# Planned Enhancements - 2026-03-03

This document outlines the planned UI enhancements for today, focusing on the TextScramble effect and specific page display issues.

## 1. Text Typing Effect Speed
- **Goal**: Make the character changing/scrambling slower for a more deliberate look.
- **Action Points**:
    - [x] Increase `totalFrames` in `TextScramble.tsx` to `Math.max(100, letters.length * 5)`.

## 2. Home Page: Headline Effect
- **Goal**: Apply the text typing effect to the primary hero headline.
- **Action Points**:
    - [x] Wrapped "Make tomorrow's technology with us!" in the `TextScramble` component in `app/page.tsx`.

## 3. Careers Page: Title Clipping
- **Goal**: Ensure the letter 'g' in "Build the future of engineering." is not cut off at the bottom.
- **Action Points**:
    - [x] In `TextScramble.tsx`, changed `overflow: hidden` to `overflow: visible` on the container span.
    - [x] Added `padding-bottom: 0.25em` to the container span to accommodate descenders.

## 4. Blog Page: Title Flash
- **Goal**: Prevent the "white flash" perceived during the scramble of "Technical perspectives."
- **Action Points**:
    - [x] Stabilized the title with `whitespace-nowrap` in `app/blog/page.tsx` to prevent layout shifts during scramble.
    - [x] Verified scramble color `rgba(255, 199, 44, 0.65)` in `TextScramble.tsx`.
