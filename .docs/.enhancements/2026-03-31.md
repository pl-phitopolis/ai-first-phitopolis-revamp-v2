# AI Day Enhancements - March 31, 2026

## Overview
This document logs the requested enhancements for the `/ai-day` page to be implemented. These changes focus on refining section behaviors, updating content, adding new media, and adjusting visibility for specific sections. This serves as the implementation plan to prepare for subsequent AI-assisted development.

## Section Updates & Implementation Plan

### 1. General Page Behavior (All Sections)
- **Scroll Behavior**: Fix the showing of contents on scroll. The speed should be average (not necessarily faster) and require less overall effort, but the scrolling must feel continuous rather than heavily section-locked.
  - *Implementation Notes*: Drastically reduced the total vertical height (`vh`) allocated to scroll-pinned sections (e.g., Timeline from 700vh to 480vh, People from 480vh to 280vh, Services/Process correspondingly reduced). Lowered the GSAP `scrub` durations (e.g., from 1.5/1.8 down to 0.8) to snap animations faster to the scroll position, significantly reducing friction.

### 2. Hero Section
- **Content Removal**: Remove the narrator AI placeholder.
  - *Implementation Notes*: Locate the `sec-hero` section in `app/ai-day/page.tsx` and comment out or remove the JSX block corresponding to the narrator AI.

### 3. Story Section
- **Background Media**: Add a video background with low opacity.
  - **Asset Path**: `public/story/` (create this directory if it doesn't exist).
  - **Fallback/Stock**: Use a stock looped video of "two guys shaking hands in a business setting" if a specific asset isn't provided.
  - *Implementation Notes*: Add a `<video autoPlay loop muted playsInline>` tag fixed behind the content in `sec-statement` (Story). Ensure CSS `opacity` is adjusted to maintain high text readability.

### 4. Values Section
- **Dynamic Background Typography**: The current lowkey background text (which currently says "values") should change dynamically to match the specific content currently presented (e.g., the background text should spell out "integrity" when the user is at the integrity slide).
  - *Implementation Notes*: Implemented a map of dynamic GSAP `ghostWatermarkRefs` where each value phase has its own absolutely positioned typography (spelling out `{ph.label.toLowerCase()}`) that fades in/out based on the active scroll index. Additionally, minor UI tweaks were made to the value image containers (centered alignment, `flexShrink: 0`, and `objectPosition: 'center'`).

### 5. Services Section
- **Scroll Tuning**: Tune the scroll behavior so contents show faster with less scrolling distance.
- **Scroll Stop**: The scroll should finish/stop on the **last image and text** before transitioning to the next section (Tech Stack).
  - *Implementation Notes*: Adjust the Framer Motion `useScroll` mapping or GSAP `ScrollTrigger` in `sec-services`. Ensure the final timeline state pins the last slide visibly before the viewport unpins and moves past the section block.

### 6. Tech Stack Section
- **Content Expansion**: The "Languages and Frameworks" filter lacks enough samples. 
- **Additions**: Add the following items from the Top 20 list (duplicates already present in the codebase like TypeScript, React, Python, Java, etc. have been filtered out):
  1. JavaScript
  2. Express.js
  3. React Native
  4. Swift
  5. Kotlin
  - *Implementation Notes*: Distribute these new additions (and manually redistribute some of the existing `dev` category items) across `TECH_ROW1`, `TECH_ROW2`, and `TECH_ROW3`. This ensures that when the "Languages & Frameworks" filter is clicked, active items are spread evenly across all three marquee tracks, preventing any single track from appearing entirely grayscaled. Ensure logo fetching is programmatically set up for these new identifiers via SimpleIcons (`SI` mapping).

### 7. Demographics Section
- **Fixed Percentages**: Apply exact percentages for courses and disciplines:
  - Computer Science: 45%
  - Sciences: 12%
  - Mathematics and Statistic: 10%
  - Engineering: 10%
  - Business and Management: 8%
  - Finance and Economics: 5%
  - Accountancy: 5%
  - *(Total: 95%)*
- **QS Ranking**: Add a new stat for "37% QS Top 1000 school educated" (Source: QS World University Rankings).
  - *Implementation Notes*: Find the demographics visualization area and update the hardcoded numbers, labels, and percentage visualizers to match these new figures.

### 8. Showcases Section
- **Visibility**: Hide this section for now, but **do not delete** the code.
  - *Implementation Notes*: Apply conditional rendering (e.g., `{false && <section id="sec-showcase">...</section>}`) or `display: 'none'` to the Showcases section wrapper.

### 9. Closing Section
- **Visibility**: Hide this section for now, but **do not delete** the code.
  - *Implementation Notes*: Apply conditional rendering or `display: 'none'` to the `sec-closing` section wrapper, similar to the Showcases section.
